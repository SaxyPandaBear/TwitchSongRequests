/*
// To declare projects as part of a multi-project build use the 'include' method
include 'shared'
include 'api'
include 'services:webservice'
*/

rootProject.name = 'twitch-song-requests'
include 'server', 'lib', 'queue-song-lambda', 'passive-disconnect-lambda'