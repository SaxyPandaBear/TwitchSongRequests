{
    "name": "twitch-song-requests-lambda",
    "author": "Andrew Huynh",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/SaxyPandaBear/TwitchSongRequests.git"
    },
    "description": "Lambda code that receives messages from SQS, uses metadata from the SQS message to read data from DynamoDB, and then uses that to make an authenticated HTTP request to add a Spotify song URI to the specified player's queue.",
    "dependencies": {
        "aws-sdk": "^2.744.0",
        "aws-xray-sdk-core": "^3.1.0",
        "node-fetch": "^2.6.0"
    }
}
