plugins {
    id "com.github.node-gradle.node" version "3.0.1"
}

task assemble(type: NpmTask) {
    dependsOn npmInstall
    args = ['run', 'build']
}

task test(type: Test) {
    logger.log(LogLevel.WARN, 'No build task for CDK currently')
}
