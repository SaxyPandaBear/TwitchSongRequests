plugins {
    id "com.github.node-gradle.node" version "3.0.1"
}

task assemble(type: Task) {
    logger.log(LogLevel.WARN, 'No build task for BFF currently')
}

task test(type: Task) {
    logger.log(LogLevel.WARN, 'No test task for BFF currently')
}
